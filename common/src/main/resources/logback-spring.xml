<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <property resource="logback-variables.properties"/>

    <timestamp key="TODAY" datePattern="yyyy-MM-dd"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <Pattern>
                ${CONSOLE_LOG_PATTERN_COLOR}
            </Pattern>
        </encoder>
    </appender>

    <appender name="AUTH_LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        <file>${LOG_PATH}/${AUTH_LOG_FILE_NAME}.log</file>

        <!-- 회원 기능은 시간 기준 롤링 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${TODAY}/${AUTH_LOG_FILE_NAME}-%d{yyyy-MM-dd-HH}.log</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="PICKCAR_LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        <file>${LOG_PATH}/${PICKCAR_LOG_FILE_NAME}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${TODAY}/${PICKCAR_LOG_FILE_NAME}-%d{yyyy-MM-dd-HH}.%i.log</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 프로젝트 전체적인 에러 추적용 appender -->
    <appender name="ERROR_LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        <file>${LOG_PATH}/${ERROR_LOG_FILE_NAME}.log</file>

        <!-- 로깅 레벨 필터를 통해 WARN, ERROR만 기록함 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${TODAY}/${ERROR_LOG_FILE_NAME}-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <logger name="com.pickcar.auth" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="AUTH_LOG_FILE"/>
        <appender-ref ref="ERROR_LOG_FILE"/>
    </logger>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ERROR_LOG_FILE"/>
    </root>

    <!-- 별도 로거 설정을 하지 않는 나머지 도메인 내용들 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="PICKCAR_LOG_FILE"/>
    </root>

</configuration>